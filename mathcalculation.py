# import math
# r=float(input("Enter radious : "))
# print(f"circumference the circle is :{round(2*math.pi*r,2)}")

# #area of a circle
# import math 
# r=float(input("Enter the radious :"))
# print(f"Area of a circle :{round(math.pi*pow(r,2),2)}")

# c=sqrt(a^2+b^2)

import math
a=float(input("Enter the value of a :"))
b=float(input("Enter the value of b :"))
print(f"value of the c is: {round(math.sqrt(pow(a,2)+pow(b,2)),2)}")